import {center} from '../../styles/main';

export default ({
    container: {
        ...center,
        flexDirection: 'row',
        flexWrap: 'wrap',
        justifyContent: 'space-around',
        margin: 1
    }
});